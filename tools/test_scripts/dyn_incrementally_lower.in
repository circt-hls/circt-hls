#!/usr/bin/env bash

# First argument is the source C file to lower. Output files are emitted
# in the working directory of the script.

set -e
basename=${1##*/}
basename=${basename%.*}

printf "= Dynamically scheduled HLS'ing $1...\n"
fud exec --from c --to mlir-affine -o ${basename}_affine.mlir $1
printf "\tLowered to affine! (Polygeist)...!"
fud exec --from mlir-affine --to mlir-scf-while -o ${basename}_scf.mlir ${basename}_affine.mlir
printf "\r\tLowered to scf...!"
fud exec --from mlir-scf-while --to mlir-handshake-buffered -o ${basename}_handshake.mlir ${basename}_scf.mlir
printf "\r\tLowered to handshake...!"
fud exec --from mlir-handshake-buffered --to mlir-firrtl -o ${basename}_firrtl.mlir ${basename}_handshake.mlir
printf "\r\tLowered to FIRRTL...!"
fud exec --from mlir-firrtl --to synth-verilog -o ${basename}_sv.sv ${basename}_firrtl.mlir
